{
  "name": "foodio",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "type-check": "npm run type-check -w backend && npm run type-check -w frontend",
    "validate": "npm run validate -w backend && npm run validate -w frontend",
    "test": "npm run test -w backend",
    "docker:dev": "docker compose -f docker-compose.dev.yml up",
    "docker:dev:down": "docker compose -f docker-compose.dev.yml down",
    "docker:prod": "docker compose -f docker-compose.yml up -d",
    "docker:prod:down": "docker compose -f docker-compose.yml down",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "prettier": "^3.0.0"
  }
}
